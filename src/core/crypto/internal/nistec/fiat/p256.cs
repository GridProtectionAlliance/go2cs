// Copyright 2021 The Go Authors. All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.
// Code generated by generate.go. DO NOT EDIT.
global using p256UntypedFieldElement = go.array<uint64>;

namespace go.crypto.@internal.nistec;

using subtle = crypto.subtle_package;
using errors = errors_package;
using crypto;

partial class fiat_package {

// P256Element is an integer modulo 2^256 - 2^224 + 2^192 + 2^96 - 1.
//
// The zero value is a valid zero element.
[GoType] partial struct P256Element {
    // Values are represented internally always in the Montgomery domain, and
    // converted in Bytes and SetBytes.
    internal p256MontgomeryDomainFieldElement x;
}

internal static readonly UntypedInt p256ElementLen = 32;

// One sets e = 1, and returns e.
[GoRecv("capture")] public static ж<P256Element> One(this ref P256Element e) {
    p256SetOne(Ꮡ(e.x));
    return OneꓸᏑe;
}

// Equal returns 1 if e == t, and zero otherwise.
[GoRecv] public static nint Equal(this ref P256Element e, ж<P256Element> Ꮡt) {
    ref var t = ref Ꮡt.val;

    var eBytes = e.Bytes();
    var tBytes = t.Bytes();
    return subtle.ConstantTimeCompare(eBytes, tBytes);
}

// IsZero returns 1 if e == 0, and zero otherwise.
[GoRecv] public static nint IsZero(this ref P256Element e) {
    var zero = new slice<byte>(p256ElementLen);
    var eBytes = e.Bytes();
    return subtle.ConstantTimeCompare(eBytes, zero);
}

// Set sets e = t, and returns e.
[GoRecv("capture")] public static ж<P256Element> Set(this ref P256Element e, ж<P256Element> Ꮡt) {
    ref var t = ref Ꮡt.val;

    e.x = t.x;
    return SetꓸᏑe;
}

// Bytes returns the 32-byte big-endian encoding of e.
[GoRecv] public static slice<byte> Bytes(this ref P256Element e) {
    // This function is outlined to make the allocations inline in the caller
    // rather than happen on the heap.
    ref var out = ref heap(new array<byte>(32), out var Ꮡout);
    return e.bytes(Ꮡ@out);
}

[GoRecv] public static slice<byte> bytes(this ref P256Element e, ж<array<byte>> Ꮡout) {
    ref var @out = ref Ꮡout.val;

    ref var tmp = ref heap(new p256NonMontgomeryDomainFieldElement(), out var Ꮡtmp);
    p256FromMontgomery(Ꮡtmp, Ꮡ(e.x));
    p256ToBytes(Ꮡout, ((ж<p256UntypedFieldElement>)(Ꮡtmp)));
    p256InvertEndianness(@out[..]);
    return @out[..];
}

// SetBytes sets e = v, where v is a big-endian 32-byte encoding, and returns e.
// If v is not 32 bytes or it encodes a value higher than 2^256 - 2^224 + 2^192 + 2^96 - 1,
// SetBytes returns nil and an error, and e is unchanged.
[GoRecv("capture")] public static (ж<P256Element>, error) SetBytes(this ref P256Element e, slice<byte> v) {
    if (len(v) != p256ElementLen) {
        return (default!, errors.New("invalid P256Element encoding"u8));
    }
    // Check for non-canonical encodings (p + k, 2p + k, etc.) by comparing to
    // the encoding of -1 mod p, so p - 1, the highest canonical encoding.
    slice<byte> minusOneEncoding = @new<P256Element>().Sub(
        @new<P256Element>(), @new<P256Element>().One()).Bytes();
    foreach (var (i, _) in v) {
        if (v[i] < minusOneEncoding[i]) {
            break;
        }
        if (v[i] > minusOneEncoding[i]) {
            return (default!, errors.New("invalid P256Element encoding"u8));
        }
    }
    ref var in = ref heap(new array<byte>(32), out var Ꮡin);
    copy(@in[..], v);
    p256InvertEndianness(@in[..]);
    ref var tmp = ref heap(new p256NonMontgomeryDomainFieldElement(), out var Ꮡtmp);
    p256FromBytes(((ж<p256UntypedFieldElement>)(Ꮡtmp)), Ꮡ@in);
    p256ToMontgomery(Ꮡ(e.x), Ꮡtmp);
    return (SetBytesꓸᏑe, default!);
}

// Add sets e = t1 + t2, and returns e.
[GoRecv("capture")] public static ж<P256Element> Add(this ref P256Element e, ж<P256Element> Ꮡt1, ж<P256Element> Ꮡt2) {
    ref var t1 = ref Ꮡt1.val;
    ref var t2 = ref Ꮡt2.val;

    p256Add(Ꮡ(e.x), Ꮡ(t1.x), Ꮡ(t2.x));
    return AddꓸᏑe;
}

// Sub sets e = t1 - t2, and returns e.
[GoRecv("capture")] public static ж<P256Element> Sub(this ref P256Element e, ж<P256Element> Ꮡt1, ж<P256Element> Ꮡt2) {
    ref var t1 = ref Ꮡt1.val;
    ref var t2 = ref Ꮡt2.val;

    p256Sub(Ꮡ(e.x), Ꮡ(t1.x), Ꮡ(t2.x));
    return SubꓸᏑe;
}

// Mul sets e = t1 * t2, and returns e.
[GoRecv("capture")] public static ж<P256Element> Mul(this ref P256Element e, ж<P256Element> Ꮡt1, ж<P256Element> Ꮡt2) {
    ref var t1 = ref Ꮡt1.val;
    ref var t2 = ref Ꮡt2.val;

    p256Mul(Ꮡ(e.x), Ꮡ(t1.x), Ꮡ(t2.x));
    return MulꓸᏑe;
}

// Square sets e = t * t, and returns e.
[GoRecv("capture")] public static ж<P256Element> Square(this ref P256Element e, ж<P256Element> Ꮡt) {
    ref var t = ref Ꮡt.val;

    p256Square(Ꮡ(e.x), Ꮡ(t.x));
    return SquareꓸᏑe;
}

// Select sets v to a if cond == 1, and to b if cond == 0.
[GoRecv("capture")] public static ж<P256Element> Select(this ref P256Element v, ж<P256Element> Ꮡa, ж<P256Element> Ꮡb, nint cond) {
    ref var a = ref Ꮡa.val;
    ref var b = ref Ꮡb.val;

    p256Selectznz(((ж<p256UntypedFieldElement>)(Ꮡ(v.x))), ((p256Uint1)cond),
        ((ж<p256UntypedFieldElement>)(Ꮡ(b.x))), ((ж<p256UntypedFieldElement>)(Ꮡ(a.x))));
    return SelectꓸᏑv;
}

internal static void p256InvertEndianness(slice<byte> v) {
    for (nint i = 0; i < len(v) / 2; i++) {
        (v[i], v[len(v) - 1 - i]) = (v[len(v) - 1 - i], v[i]);
    }
}

} // end fiat_package
