// Code generated by go run encgen.go -output enc_helpers.go; DO NOT EDIT.
// Copyright 2014 The Go Authors. All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.
namespace go.encoding;

using reflect = reflect_package;

partial class gob_package {

internal static map<reflectꓸKind, Func<ж<encoderState>, reflectꓸValue, bool>> encArrayHelper = new map<reflectꓸKind, Func<ж<encoderState>, reflectꓸValue, bool>>{
    [reflect.ΔBool] = encBoolArray,
    [reflect.Complex64] = encComplex64Array,
    [reflect.Complex128] = encComplex128Array,
    [reflect.Float32] = encFloat32Array,
    [reflect.Float64] = encFloat64Array,
    [reflect.ΔInt] = encIntArray,
    [reflect.Int16] = encInt16Array,
    [reflect.Int32] = encInt32Array,
    [reflect.Int64] = encInt64Array,
    [reflect.Int8] = encInt8Array,
    [reflect.ΔString] = encStringArray,
    [reflect.ΔUint] = encUintArray,
    [reflect.Uint16] = encUint16Array,
    [reflect.Uint32] = encUint32Array,
    [reflect.Uint64] = encUint64Array,
    [reflect.Uintptr] = encUintptrArray
};

internal static map<reflectꓸKind, Func<ж<encoderState>, reflectꓸValue, bool>> encSliceHelper = new map<reflectꓸKind, Func<ж<encoderState>, reflectꓸValue, bool>>{
    [reflect.ΔBool] = encBoolSlice,
    [reflect.Complex64] = encComplex64Slice,
    [reflect.Complex128] = encComplex128Slice,
    [reflect.Float32] = encFloat32Slice,
    [reflect.Float64] = encFloat64Slice,
    [reflect.ΔInt] = encIntSlice,
    [reflect.Int16] = encInt16Slice,
    [reflect.Int32] = encInt32Slice,
    [reflect.Int64] = encInt64Slice,
    [reflect.Int8] = encInt8Slice,
    [reflect.ΔString] = encStringSlice,
    [reflect.ΔUint] = encUintSlice,
    [reflect.Uint16] = encUint16Slice,
    [reflect.Uint32] = encUint32Slice,
    [reflect.Uint64] = encUint64Slice,
    [reflect.Uintptr] = encUintptrSlice
};

internal static bool encBoolArray(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    // Can only slice if it is addressable.
    if (!v.CanAddr()) {
        return false;
    }
    return encBoolSlice(Ꮡstate, v.Slice(0, v.Len()));
}

internal static bool encBoolSlice(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    var (Δslice, ok) = v.Interface()._<slice<bool>>(ᐧ);
    if (!ok) {
        // It is kind bool but not type bool. TODO: We can handle this unsafely.
        return false;
    }
    foreach (var (_, x) in Δslice) {
        if (x != false || state.sendZero) {
            if (x){
                state.encodeUint(1);
            } else {
                state.encodeUint(0);
            }
        }
    }
    return true;
}

internal static bool encComplex64Array(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    // Can only slice if it is addressable.
    if (!v.CanAddr()) {
        return false;
    }
    return encComplex64Slice(Ꮡstate, v.Slice(0, v.Len()));
}

internal static bool encComplex64Slice(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    var (Δslice, ok) = v.Interface()._<slice<complex64>>(ᐧ);
    if (!ok) {
        // It is kind complex64 but not type complex64. TODO: We can handle this unsafely.
        return false;
    }
    foreach (var (_, x) in Δslice) {
        if (x != 0 + builtin.i(0F) || state.sendZero) {
            var rpart = floatBits((float64)real(x));
            var ipart = floatBits((float64)imag(x));
            state.encodeUint(rpart);
            state.encodeUint(ipart);
        }
    }
    return true;
}

internal static bool encComplex128Array(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    // Can only slice if it is addressable.
    if (!v.CanAddr()) {
        return false;
    }
    return encComplex128Slice(Ꮡstate, v.Slice(0, v.Len()));
}

internal static bool encComplex128Slice(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    var (Δslice, ok) = v.Interface()._<slice<complex128>>(ᐧ);
    if (!ok) {
        // It is kind complex128 but not type complex128. TODO: We can handle this unsafely.
        return false;
    }
    foreach (var (_, x) in Δslice) {
        if (x != 0 + builtin.i(0F) || state.sendZero) {
            var rpart = floatBits(real(x));
            var ipart = floatBits(imag(x));
            state.encodeUint(rpart);
            state.encodeUint(ipart);
        }
    }
    return true;
}

internal static bool encFloat32Array(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    // Can only slice if it is addressable.
    if (!v.CanAddr()) {
        return false;
    }
    return encFloat32Slice(Ꮡstate, v.Slice(0, v.Len()));
}

internal static bool encFloat32Slice(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    var (Δslice, ok) = v.Interface()._<slice<float32>>(ᐧ);
    if (!ok) {
        // It is kind float32 but not type float32. TODO: We can handle this unsafely.
        return false;
    }
    foreach (var (_, x) in Δslice) {
        if (x != 0 || state.sendZero) {
            var bits = floatBits((float64)x);
            state.encodeUint(bits);
        }
    }
    return true;
}

internal static bool encFloat64Array(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    // Can only slice if it is addressable.
    if (!v.CanAddr()) {
        return false;
    }
    return encFloat64Slice(Ꮡstate, v.Slice(0, v.Len()));
}

internal static bool encFloat64Slice(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    var (Δslice, ok) = v.Interface()._<slice<float64>>(ᐧ);
    if (!ok) {
        // It is kind float64 but not type float64. TODO: We can handle this unsafely.
        return false;
    }
    foreach (var (_, x) in Δslice) {
        if (x != 0 || state.sendZero) {
            var bits = floatBits(x);
            state.encodeUint(bits);
        }
    }
    return true;
}

internal static bool encIntArray(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    // Can only slice if it is addressable.
    if (!v.CanAddr()) {
        return false;
    }
    return encIntSlice(Ꮡstate, v.Slice(0, v.Len()));
}

internal static bool encIntSlice(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    var (Δslice, ok) = v.Interface()._<slice<nint>>(ᐧ);
    if (!ok) {
        // It is kind int but not type int. TODO: We can handle this unsafely.
        return false;
    }
    foreach (var (_, x) in Δslice) {
        if (x != 0 || state.sendZero) {
            state.encodeInt((int64)x);
        }
    }
    return true;
}

internal static bool encInt16Array(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    // Can only slice if it is addressable.
    if (!v.CanAddr()) {
        return false;
    }
    return encInt16Slice(Ꮡstate, v.Slice(0, v.Len()));
}

internal static bool encInt16Slice(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    var (Δslice, ok) = v.Interface()._<slice<int16>>(ᐧ);
    if (!ok) {
        // It is kind int16 but not type int16. TODO: We can handle this unsafely.
        return false;
    }
    foreach (var (_, x) in Δslice) {
        if (x != 0 || state.sendZero) {
            state.encodeInt((int64)x);
        }
    }
    return true;
}

internal static bool encInt32Array(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    // Can only slice if it is addressable.
    if (!v.CanAddr()) {
        return false;
    }
    return encInt32Slice(Ꮡstate, v.Slice(0, v.Len()));
}

internal static bool encInt32Slice(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    var (Δslice, ok) = v.Interface()._<slice<int32>>(ᐧ);
    if (!ok) {
        // It is kind int32 but not type int32. TODO: We can handle this unsafely.
        return false;
    }
    foreach (var (_, x) in Δslice) {
        if (x != 0 || state.sendZero) {
            state.encodeInt((int64)x);
        }
    }
    return true;
}

internal static bool encInt64Array(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    // Can only slice if it is addressable.
    if (!v.CanAddr()) {
        return false;
    }
    return encInt64Slice(Ꮡstate, v.Slice(0, v.Len()));
}

internal static bool encInt64Slice(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    var (Δslice, ok) = v.Interface()._<slice<int64>>(ᐧ);
    if (!ok) {
        // It is kind int64 but not type int64. TODO: We can handle this unsafely.
        return false;
    }
    foreach (var (_, x) in Δslice) {
        if (x != 0 || state.sendZero) {
            state.encodeInt(x);
        }
    }
    return true;
}

internal static bool encInt8Array(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    // Can only slice if it is addressable.
    if (!v.CanAddr()) {
        return false;
    }
    return encInt8Slice(Ꮡstate, v.Slice(0, v.Len()));
}

internal static bool encInt8Slice(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    var (Δslice, ok) = v.Interface()._<slice<int8>>(ᐧ);
    if (!ok) {
        // It is kind int8 but not type int8. TODO: We can handle this unsafely.
        return false;
    }
    foreach (var (_, x) in Δslice) {
        if (x != 0 || state.sendZero) {
            state.encodeInt((int64)x);
        }
    }
    return true;
}

internal static bool encStringArray(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    // Can only slice if it is addressable.
    if (!v.CanAddr()) {
        return false;
    }
    return encStringSlice(Ꮡstate, v.Slice(0, v.Len()));
}

internal static bool encStringSlice(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    var (Δslice, ok) = v.Interface()._<slice<@string>>(ᐧ);
    if (!ok) {
        // It is kind string but not type string. TODO: We can handle this unsafely.
        return false;
    }
    foreach (var (_, x) in Δslice) {
        if (x != ""u8 || state.sendZero) {
            state.encodeUint((uint64)len(x));
            state.b.WriteString(x);
        }
    }
    return true;
}

internal static bool encUintArray(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    // Can only slice if it is addressable.
    if (!v.CanAddr()) {
        return false;
    }
    return encUintSlice(Ꮡstate, v.Slice(0, v.Len()));
}

internal static bool encUintSlice(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    var (Δslice, ok) = v.Interface()._<slice<nuint>>(ᐧ);
    if (!ok) {
        // It is kind uint but not type uint. TODO: We can handle this unsafely.
        return false;
    }
    foreach (var (_, x) in Δslice) {
        if (x != 0 || state.sendZero) {
            state.encodeUint((uint64)x);
        }
    }
    return true;
}

internal static bool encUint16Array(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    // Can only slice if it is addressable.
    if (!v.CanAddr()) {
        return false;
    }
    return encUint16Slice(Ꮡstate, v.Slice(0, v.Len()));
}

internal static bool encUint16Slice(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    var (Δslice, ok) = v.Interface()._<slice<uint16>>(ᐧ);
    if (!ok) {
        // It is kind uint16 but not type uint16. TODO: We can handle this unsafely.
        return false;
    }
    foreach (var (_, x) in Δslice) {
        if (x != 0 || state.sendZero) {
            state.encodeUint((uint64)x);
        }
    }
    return true;
}

internal static bool encUint32Array(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    // Can only slice if it is addressable.
    if (!v.CanAddr()) {
        return false;
    }
    return encUint32Slice(Ꮡstate, v.Slice(0, v.Len()));
}

internal static bool encUint32Slice(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    var (Δslice, ok) = v.Interface()._<slice<uint32>>(ᐧ);
    if (!ok) {
        // It is kind uint32 but not type uint32. TODO: We can handle this unsafely.
        return false;
    }
    foreach (var (_, x) in Δslice) {
        if (x != 0 || state.sendZero) {
            state.encodeUint((uint64)x);
        }
    }
    return true;
}

internal static bool encUint64Array(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    // Can only slice if it is addressable.
    if (!v.CanAddr()) {
        return false;
    }
    return encUint64Slice(Ꮡstate, v.Slice(0, v.Len()));
}

internal static bool encUint64Slice(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    var (Δslice, ok) = v.Interface()._<slice<uint64>>(ᐧ);
    if (!ok) {
        // It is kind uint64 but not type uint64. TODO: We can handle this unsafely.
        return false;
    }
    foreach (var (_, x) in Δslice) {
        if (x != 0 || state.sendZero) {
            state.encodeUint(x);
        }
    }
    return true;
}

internal static bool encUintptrArray(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    // Can only slice if it is addressable.
    if (!v.CanAddr()) {
        return false;
    }
    return encUintptrSlice(Ꮡstate, v.Slice(0, v.Len()));
}

internal static bool encUintptrSlice(ж<encoderState> Ꮡstate, reflectꓸValue v) {
    ref var state = ref Ꮡstate.Value;

    var (Δslice, ok) = v.Interface()._<slice<uintptr>>(ᐧ);
    if (!ok) {
        // It is kind uintptr but not type uintptr. TODO: We can handle this unsafely.
        return false;
    }
    foreach (var (_, x) in Δslice) {
        if (x != 0 || state.sendZero) {
            state.encodeUint((uint64)x);
        }
    }
    return true;
}

} // end gob_package
